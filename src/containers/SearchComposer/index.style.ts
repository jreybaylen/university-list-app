import { StypesProps } from '@interface/style.interface'

const styles: StypesProps = {
    container: {
        padding: 20,
        maxWidth: 600,
        width: '100%',
        margin: 'auto'
    },
    searchForm: {
        display: 'flex'
    },
    input: {
        textTransform: 'capitalize'
    }
}

export {
    styles
}