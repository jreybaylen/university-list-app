import { StypesProps } from '@interface/style.interface'

const styles: StypesProps = {
    container: {
        padding: 20,
        width: '100%',
        margin: 'auto',
        maxWidth: '600px'
    },
    searchForm: {
        display: 'flex'
    }
}

export {
    styles
}