import { StypesProps } from '@interface/style.interface'

const styles: StypesProps = {
    container: {
        padding: 20,
        width: '100%',
        margin: 'auto',
        maxWidth: 1000,
        fontWeight: 500,
        fontSize: '1.2rem',
        textAlign: 'center'
    }
}

export {
    styles
}